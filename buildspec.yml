version: 0.2

phases:
  pre_build:
    commands:
      - echo "ENVIRONMENT variable is set to $ENVIRONMENT"
      - echo "REPOSITORY_URI variable is set to $REPOSITORY_URI"
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin $REPOSITORY_URI
  build:
    commands:
      - echo Building the Docker image...
      - docker build -t quest-api:$ENVIRONMENT .
      - docker tag quest-api:$ENVIRONMENT $REPOSITORY_URI/quest-api:$ENVIRONMENT
  post_build:
    commands:
      - echo Pushing the Docker image...
      - docker push $REPOSITORY_URI/quest-api:$ENVIRONMENT
      - echo Writing image definitions file...
      - printf '[{"name":"quest-api","imageUri":"%s/quest-api:%s"}]' $REPOSITORY_URI $ENVIRONMENT > imagedefinitions.json
artifacts:
  files:
    - imagedefinitions.json
